{
  "name": "@openclaw/server-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker deployment for the OpenClaw v2 pairing server",
  "scripts": {
    "check": "node --check src/index.js && node --check src/pair-v2-hub.js && node --check src/protocol-site.js && node --check src/shared.js",
    "dev": "npx --yes wrangler@4.57.0 dev",
    "deploy": "npx --yes wrangler@4.57.0 deploy"
  }
}
